<%= form_tag(inventory_path(inventory.id), method: :delete, class: 'display-inline') do %>
  <%= hidden_field_tag :id, inventory.id %>
  <%= submit_tag '削除', class: 'btn btn-default btn-xs', data: { confirm: '本当に削除してよろしいですか？' }  %>
<% end %>
