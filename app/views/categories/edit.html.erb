<h1>id: <%= @category.id %> の物品カテゴリ名編集</h1>

<%= form_for(@category) do |f| %>
  <%= f.label :name, 'カテゴリ名' %>
  <%= f.text_field :name %>

  <%= f.submit '更新' %>
<% end %>

<%= link_to 'カテゴリ一覧に戻る', categories_path, class: 'btn btn-default btn-sm' %>
